# autotests_local — manual (@Tag(manual) — exploratory stubs)
# ./gradlew testManual -DpyramidStand=autotests_local

# Allow attachments after each test
attachBrowserConsoleLogs=false
attachHarLogs=false
attachLastScreenshot=true
attachPageSource=true
attachVideo=false
# Allow allure steps listener
enableAllureSelenideListener=true

# Target app
baseUrl=http://localhost:8081/

# REST API
apiBaseUrl=http://localhost:8081/

# Browser configuration
browser=chrome
browserVersion=148
browserSize=1920x1280
headless=false
closeBrowserAfterEach=true
closeBrowserAfterAll=true

# Remote browser hub configuration
enableHar=false
enableVnc=false
enableVideo=false
# videoFolder=
# remoteUrl=

# Console log
logToConsole=true
selenideLogToConsole=true
rootLogLevel=info
